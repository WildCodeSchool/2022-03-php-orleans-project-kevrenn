{% extends 'layout.html.twig' %}

{% block content %}
    <h1>event {{ event.title }}</h1>
    <p>{{ event.id }}. {{ event.title }}</p>
    <a href="/admin/evenements/edit?id={{ event.id }}">Edit</a>
    <form action="/events/delete" method="POST">
        <input type="hidden" name="id" value="{{ event.id }}">
        <button class="btn disabled">Delete</button>
    </form>
    <a href="/admin/evenements">Back</a>
{% endblock %}